* Find out which ISA ethernet cards discover(2) can probe safely, and add
  them to nic-shared-modules or their own package. There is still 100+k free
  on the net_drivers floppy.
* The lists should be sorted, so if we need to trim space we can remove
  from the end.
* Docs for making a custom modules package, or using a different or custom
  kernel.
* Generate module-deps file from deps in debian/control so it does not need
  to be kept sorted and in sync.

<!-- original version: 28997 -->
<!-- revised by Marco Carvalho (macs) 2006.01.01 -->

  <sect2 arch="sparc" id="boot-tftp"><title>Inicializando a partir do TFTP</title>

&boot-installer-intro-net.xml;

<para>

Em mÃ¡quinas com o OpenBoot, simplesmente entre no monitor de
inicializaÃ§Ã£o da mÃ¡quina que esta sendo instalada (veja
<xref linkend="invoking-openboot"/>).
Use o comando <userinput>boot net</userinput> para inicializar atravÃ©s de
um servidor TFTP
e RArP ou tente <userinput>boot net:bootp</userinput> ou
<userinput>boot net:dhcp</userinput> para inicializar atravÃ©s de um servidor
TFTP, BOOTP ou DHCP. Alguma revisÃµes antigas do OpenBoot requerem o uso do
nome de dispositivo, tal como <userinput>boot le()</userinput>;
estes provavelmente nÃ£o suportam BOOTP e nem DHCP.

</para>
  </sect2>


  <sect2 arch="sparc"><title>Inicializando atravÃ©s de um CD-ROM</title>

&boot-installer-intro-cd.xml;

<para>

A maioria das versÃµes do OpenBoot suportam o comando
<userinput>boot cdrom</userinput>
que Ã© simplesmente um alias para inicializaÃ§Ã£o atravÃ©s de um dispositivo
SCSI na ID 6(ou principal secundÃ¡rio para sistemas baseados em IDE). VocÃª
poderÃ¡ ter que usar o nome de dispositivo atual para versÃµes antigas
do OpenBoot que nÃ£o suportam este comando especial. Note que o mesmo
problema foi reportado na Sun4m (e.g., Sparc 10s e Sparc 20s)
durante a inicializaÃ§Ã£o via CD-ROM.

</para>
  </sect2>

  <sect2 arch="sparc" condition="supports-floppy-boot">
  <title>Inicializando atravÃ©s de Disquetes</title>
<para>

Para inicializar atravÃ©s de disquetes na Sparc ,use
<informalexample><screen>
Stop-A -> OpenBoot: "boot floppy"
</screen></informalexample>

Esteja alerta que novas arquiteturas Sun4u (ultra) nÃ£o suportam a
inicializaÃ§Ã£o via disquetes. Uma mensagem de erro tÃ­pica Ã©
<computeroutput>Bad magic number in disk label - Can't open disk label
package</computeroutput>.
Ainda mais, um grande nÃºmero de modelos (tal como o IPX) nÃ£o suporta
as imagens compactadas encontradas no discos, assim tambÃ©m nÃ£o sÃ£o
suportadas.

</para><para>

Diversas Sparcs (e.g. Ultra 10) tem um bug OBP que impede
que elas iniciem (ao invÃ©s de nÃ£o suportar totalmente a inicializaÃ§Ã£o).
A atualizaÃ§Ã£o apropriada do OBP pode ser baixada como produto com ID 106121
atravÃ©s do link <ulink url="http://sunsolve.sun.com"></ulink>.

</para><para>

Se estiver inicializando atravÃ©s de disquetes e ver mensagens como
<informalexample><screen>
Fatal error: Cannot read partition
Illegal or malformed device name
</screen></informalexample>
Ã© possÃ­vel que a inicializaÃ§Ã£o via disquetes nÃ£o seja suportada
em sua mÃ¡quina.

</para>
  </sect2>

  <sect2 arch="sparc"><title>Mensagens do IDPROM</title>
<para>

Caso nÃ£o puder inicializar porque vocÃª estÃ¡ recebendo mensagens
sobre um problema com a <quote>IDPROM</quote>, entÃ£o Ã© possÃ­vel que sua bateria
NVRAM, que armazena a informaÃ§Ã£o de configuraÃ§Ã£o de seu firmware,
esteja esgotada. Veja <ulink url="&url-sun-nvram-faq;">Sun NVRAM FAQ</ulink>
para mais detalhes.

</para>
  </sect2>

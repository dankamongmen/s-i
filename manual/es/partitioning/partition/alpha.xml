<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- original version: 39920 -->
<!-- Revisado por Steve Langasek -->

  <sect2 arch="alpha"><title>Particionado en &arch-title;</title>
<para>

Para arrancar Debian desde la consola SRM (el único método de arranque
soportado por &releasename;) es preciso una etiqueta de discos de tipo
BSD, no una tabla de particiones de tipo DOS, en su disco de arranque.
(Recuerde que el bloque de inicio de tipo SRM es totalmente incompatible
con las tablas de particiones de tipo MS-DOS;
consulte <xref linkend="alpha-firmware"/>) para más información.
Como consecuencia,
<command>partman</command> crea una etiqueta de discos BSD cuando se
ejecuta en &architecture;. Las particiones existentes tendrán que 
borrarse antes de que <command>partman</command> pueda converti su disco
para que utilice una etiqueta de discos si su disco ya tiene una tabla de
particiones de tipo DOS.

</para><para>

Debe usar la orden <quote>b</quote> para modificar la etiqueta de
disco si ha decidido usar <command>fdisk</command> para particionar su
disco y el disco que ha seleccionado para particionar no contiene una
etiqueta de disco de tipo BSD.

</para><para>

<emphasis>No</emphasis> debe crear
la tercera partición como una partición de <quote>disco completo</quote>, es
decir, de tal forma que los sectores de inicio y fin cubran el total de
capacidad del disco a menos que desee usar el disco que está particionando con
Tru64 Unix o alguno de los sistemas derivados de Free 4.4BSD-Lite (FreeBSD,
OpenBSD, o NetBSD). Si lo hace así el disco no será compatible
con las herramientas utilizadas por <command>aboot</command> para
hacer que el disco pueda arrancar. 
Esto significa que el disco configurado por el instalador como disco
de arranque de Debian no será accesible a los sistemas operativos antes
mecionados.

</para><para>

Además, debido a que <command>aboot</command> está instalado
en los primeros sectores del disco (actualmente ocupa cerca de 70 kilobytes,
o 150 sectores), <emphasis>debe</emphasis> dejar suficiente espacio
al principio del disco. Tiempo atrás, era recomendable crear
una pequeña partición al principio del disco y dejarla sin formatear.
Por la misma razón que la primera, sugerimos que no haga esto en discos
que tendrán solamente sistemas GNU/Linux.  Cuando usa
<command>partman</command>, todavía se creará una partición pequeña
para <command>aboot</command> por motivos de conveniencia.

</para><para condition="FIXME">

Debe crear una pequeña partición de tipo FAT
al principio del disco, conteniendo <command>MILO</command> y
<command>linload.exe</command> para instalaciones ARC. 5 MB deberían ser suficiente espacio para hacer esto.
Para mayor información, vea
<xref linkend="non-debian-partitioning"/>. Desafortunadamente, todavía
no es posible hacer una partición FAT desde el menú, por lo tanto
deberá crearla desde el intérprete de órdenes, usando la orden
<command>mkdosfs</command> antes de instalar el cargador de arranque
en su máquina.

</para>
  </sect2>

#!/bin/sh

dev=$1
id=$2
method=$3

case $method in
    formatable)
	echo linux-swap
	;;
    existing)
	[ -f $id/detected_filesystem ] || exit 0
	fs=$(cat $id/detected_filesystem)
	
	case "$fs" in
	    linux-swap)
		echo linux-swap
		;;
	esac
	;;
esac



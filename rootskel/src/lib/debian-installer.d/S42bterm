bterm=/usr/bin/bterm
font=/unifont.bgf

# Load framebuffer and enable it for bterm
if [ -x "$bterm" -a -e "$font" -a C.UTF-8 = "$LANG" -a true = "$framebuffer" ] && enable-fb ; then
    use_bterm=true
fi

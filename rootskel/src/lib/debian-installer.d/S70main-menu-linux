bterm=/usr/bin/bterm
font=/unifont.bgf

if [ -x "$bterm" -a -e "$font" -a C.UTF-8 = "$LANG" -a yes = "$TERM_FRAMEBUFFER" ] ; then
	TERM=bterm
	export TERM
	RES=129
	while [ $RES -eq 129 ]  ; do
  		$bterm -f $font -l $LANG /sbin/di-main-menu
		RES=$?
	done
else
	/sbin/di-main-menu
fi

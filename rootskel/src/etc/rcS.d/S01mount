# Make sure proc and devfs is mounted when we continue.  This is for
# the archs which do not use linuxrc
if [ ! -d /proc/self ] ; then
    mount -t proc proc /proc
fi
if [ ! -e /dev/.devfsd ] ; then
    mount -t devfs devfs /dev
fi

# Make sure the kernel send Alt-Ctrl-Del to the init process
if [ -e /proc/sys/kernel/cad_pid ]; then
    pidof -s linuxrc > /proc/sys/kernel/cad_pid
else
    echo "Unable to find /proc/sys/kernel/cad_pid"
fi

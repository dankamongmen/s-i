* make --unpack and --configure be recognized, rather than -u and -c.
  Seems like this would be better, so tools that use udpkg don't need
  to call in in special ways.
* make status file read/write preserve any localized short descriptions

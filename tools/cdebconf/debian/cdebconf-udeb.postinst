#! /bin/sh -e

if [ -z "$DEBCONF_PRIORITY" ] ; then
    . /usr/share/debconf/confmodule

    # Why is this priority 'critical'?  'medium' should be high
    # enough, I think. [pere 2003-01-26]
    db_input critical debconf/priority || true
    db_go || true
fi

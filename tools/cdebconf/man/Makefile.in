include ../globalmakeflags

VPATH = $(srcdir)/man

CENTER=Debconf
MANPAGES=dpkg-preconfigure.8 dpkg-reconfigure.8 debconf.7

all: $(MANPAGES)

$(MANPAGES):
	@$(POD2MAN) --section=$(subst .,,$(suffix $@)) --center=$(CENTER) -d$(CENTER) $(srcdir)/man/$(basename $@).pod > $(basename $@).man

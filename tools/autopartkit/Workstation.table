#
# Create these partitions on Workstation installs
# MPoint   FStype       Min     Max     percent of total diskspace

# / Needs to store all downloaded .debs
/          default     1280   4928     0.0

# Disable separate /usr until libldap is moved into /lib (bug #198)
#/usr       default     1152   4096     0.0

# swap should be 2 x memory size
swap       swap           64   2048     0.0

# Workstations don't have home-directories (they will be NFS mounted
# from a server)

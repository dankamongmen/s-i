#
# Create these partitions on Workstation + LTSP-server installs
# MPoint   FStype       Min     Max

# / Needs to store all downloaded .debs
/          default      256    1024

/usr       default     1216    4096

# swap should be 2 x memory size
swap       swap          256   2048

# Workstations don't have /home (it will be NFS mounted from a server)

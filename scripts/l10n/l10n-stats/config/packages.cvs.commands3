


echo -n "    menu	..."
cvs -z3 -q -d:pserver:anonymous@cvs.alioth.debian.org:/cvsroot/menu co menu
echo "Done."


echo -n "    xorg	..."
svn -q co svn://necrotic.deadbeast.net/xorg-x11/trunk/debian/xorg/debian xserver-xorg
echo "Done."

echo -n "    exim4	..."
rm -rf exim4
mkdir exim4
cd exim4
svn -q co svn://svn.debian.org/svn/pkg-exim4/exim/trunk/debian > /dev/null 2>&1
echo "Done."

cd ../






echo -n "    menu..."
cvs -z3 -q -d:pserver:anonymous@cvs.alioth.debian.org:/cvsroot/menu co menu
echo "done."


echo -n "    xorg..."
svn -q co svn://necrotic.deadbeast.net/xorg-x11/trunk/debian/xorg/debian/po xserver-xorg/debian/po
echo "done."

echo -n "    exim4..."
rm -rf exim4
mkdir exim4
cd exim4
mkdir debian
cd debian
svn -q co svn://svn.debian.org/svn/pkg-exim4/exim/trunk/debian/po > /dev/null 2>&1
echo "done."

cd ../../



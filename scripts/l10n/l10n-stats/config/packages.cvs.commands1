PATH="$PATH:~joeyh/bin"

#d-i
echo -n "    debian-installer..."
mkdir -p debian-installer
cd debian-installer/packages
for I in `find ./ -name "*.pot"`; do
	rm -f $I 
done
svn cleanup
svn -q update 
echo "done.

cd ../..


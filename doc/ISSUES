Expected problems:

* library reduction

  It is difficult to reduce a library if you don't know what functions in it
  will be used. Since additional modules could be dropped onto install
  media at any time, it is very difficult to guarentee a reduced libc will
  have all the symbols they need.

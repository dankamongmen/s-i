* figure out the kernel version from the package; it is currently hardcoded
  in copy-modules
* support for kernel flavors
* support for autograbbing kernel package somehow ?
* ensuring the same kernel is installed onto the base system the installer
  installs.

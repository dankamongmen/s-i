#!/bin/sh

grep "^\(Package\|Version\):" /var/lib/dpkg/status | \
(
    while read tag content ; do
	if [ "$tag" = "Package:" ] ; then
	    package="$content"
	fi
	if [ "$tag" = "Version:" ] ; then
	    echo "$package $content"
	fi
    done
)

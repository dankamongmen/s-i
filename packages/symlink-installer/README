This package can be used to provide bootable-system on systems which
look for the kernel in a specific, non-standard location.  The package
handles this situation by creating a symlink to the real kernel.


#!/bin/sh
set -e
grep -v "^installer:" /etc/passwd > /etc/passwd.new
mv /etc/passwd.new /etc/passwd
grep -v "^installer:" /etc/shadow > /etc/shadow.new
mv /etc/shadow.new /etc/shadow
dpkg --purge network-console-config >/dev/null 2>&1 || true

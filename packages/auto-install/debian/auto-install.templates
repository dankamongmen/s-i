Template: auto-install/enabled
Type: boolean
Default: false
# Not translatable because it's only to be preseeded.
Description: if true, attempt a fully automatic install

Template: auto-install/defaultroot
Type: string
Default: d-i/etch/./preseed.cfg
# Not translatable because it's only to be preseeded.
Description: path added to local server to give the preseed root

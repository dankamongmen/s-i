#! /bin/sh
# Configure a single package earlier than usual (helper script, run inside
# bterm).

export UDPKG_QUIET=y
udpkg --force-configure --configure "$KICKSEED_UDPKG_PACKAGE" 2>&1 | \
	logger -t kickseed

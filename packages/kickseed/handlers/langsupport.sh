#! /bin/sh

langsupport_handler () {
	# TODO REQUIRED <languages> [--default=]
	# preseed/localechooser changes needed to allow different
	# installation and installed languages
	die "langsupport not supported yet"
}

#! /bin/sh

rootpw_handler () {
	# TODO REQUIRED [--iscrypted] <password>
	# requires password preseeding support in shadow (security
	# considerations), or password question in first stage
	die "rootpw not supported yet"
}

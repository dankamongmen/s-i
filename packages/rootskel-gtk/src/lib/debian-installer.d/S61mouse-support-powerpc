subarch="$(archdetect)"
subarch="${subarch#*/}"
if [ "$DEBIAN_FRONTEND" = gtk ] ; then
    case $subarch in
	chrp|chrp_pegasos|chrp_rs6k|chrp_ibm|prep)
	    modprobe evdev
	    modprobe psmouse
	;;
    esac
fi

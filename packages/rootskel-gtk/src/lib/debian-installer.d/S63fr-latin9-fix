# Temporary workaround for #339352

frmap=/usr/share/keymaps/i386/azerty/fr-latin9.kmap

if [ "$DEBIAN_FRONTEND" = gtk ] && [ -f $frmap.gz ]; then
	gunzip $frmap.gz
	sed -i "s/CapsShift/Shift/g" $frmap
fi

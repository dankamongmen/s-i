if [ -z "$TERM_TYPE" ]; then
	case $(tty) in
		/dev/console|/dev/ttyd[0-f]*)
			TERM_TYPE=serial
			;;
		/dev/ttyv[0-f]*)
			TERM_TYPE=virtual
			;;
		/dev/ttyp[0-f]*)
			TERM_TYPE=pts
			;;
	esac
fi

if [ -z "$TERM" ] ; then
	TERM=cons25
fi

export TERM
export TERM_TYPE

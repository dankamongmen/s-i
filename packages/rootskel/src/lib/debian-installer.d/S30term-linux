#!/bin/sh
# Source the script that does the actual work. Done this way so that
# userdevfs can source it too, from debian-installer-startup.d

. /lib/debian-installer/detect-console

export TERM_TYPE

#!/bin/sh

dev="$2"
id="$3"

cd "$dev"

[ -d "$dev/$id" ] || mkdir "$dev/$id"

echo sibyl >"$dev/$id/method"
if [ -f "$dev/$id/format" ]; then
    rm "$dev/$id/format"
fi
if [ -f "$dev/$id/use_filesystem" ]; then
    rm "$dev/$id/use_filesystem"
fi

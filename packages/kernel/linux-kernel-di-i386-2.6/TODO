* Find out which ISA ethernet cards discover(2) can probe safely, and add
  them to nic-shared-modules or their own package. There is still 100+k free
  on the net_drivers floppy.

* The 2.6 kernel is missing a lot of modules, need to get a list of all
  modules included in the udebs, and compare vs. the full module list and
  add missing stuff.

* Fix input-modules, re-add udb keyboard modules.

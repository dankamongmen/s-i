#!/bin/sh -e

# live-initramfs
PLACES="$PLACES /cdrom/live/filesystem.dir"
# casper
PLACES="$PLACES /cdrom/casper/filesystem.dir"

dir_prepare() {
	cd $place
}

dir_teardown() {
	:
}

dir_count() {
	echo `find . | wc -l`
}

#!/bin/sh

dev="$1"
id="$2"
property="$3"

case $property in
    formatable)
        echo efi
        ;;
    existing)
	[ -f $id/detected_filesystem ] || exit 0
	fs=$(cat $id/detected_filesystem)
	
	case "$fs" in
	    fat16|fat32)
		echo efi
		;;
	esac
	;;
esac

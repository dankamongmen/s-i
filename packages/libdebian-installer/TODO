* Move reverse relations handling out of parsing step.

* Remove pointer from dependency entry.
  - Needs extra reverse relations step.

* Change first parameter of parser functions to void *data

* Redo parser to use one call per entry

* Remove callback from writer functions?

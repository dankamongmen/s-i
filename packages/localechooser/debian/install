localechooser languagemap translation-check usr/bin
post-base-installer.d finish-install.d usr/lib
# The following while we have no other way to get SUPPORTED
# Should become useless when we will have it
debian/SUPPORTED-short etc
debian/short-tmp/shortlists etc

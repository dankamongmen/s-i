#!/bin/sh
set -e

. /usr/share/debconf/confmodule
. /lib/preseed/preseed.sh

if [ -e $logfile ]; then
	if ! log-output -t preseed chroot /target debconf-set-selections < $logfile; then
		error preseed/load_error $logfile
	fi
fi

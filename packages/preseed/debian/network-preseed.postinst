#!/bin/sh
set -e

. /usr/share/debconf/confmodule
. /lib/preseed/preseed.sh

dhcp_url=$(dhcp_preseed_url)
[ "$dhcp_url" ] && preseed_location "$dhcp_url"
preseed preseed/url
preseed_command preseed/early_command



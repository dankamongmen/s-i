#! /bin/sh
set -e

. /usr/share/debconf/confmodule

# Don't install Recommends by default during Lenny installs
db_fget base-installer/install-recommends seen
if [ "$RET" != true ]; then
	db_set base-installer/install-recommends false
fi
